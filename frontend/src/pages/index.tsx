import type { NextPage } from "next";
import Menu from "../components/Menu";

const Home: NextPage = () => {
  return (
    <section>
      <Menu/>
      <h1>Home</h1>
    </section>
  );
};

export default Home;
